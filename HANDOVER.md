# InnerOS 人生之书 · 项目交接文档

> 本文件写给下一位接手本仓库的 Codex / 开发者。目标是让接手者不依赖任何上下文即可继续开发、验证与发布。

---

## 1. 项目目标与当前产品定义

**项目**：InnerOS —— 一款“长期自我观察记录”产品，用 AI 帮助用户整理经历、看见反复出现的模式、理解正在变化的自己，并把可核对的洞察沉淀进一本“我的人生之书”。

**当前版本（已有用户版）**：我们演示的是已积累数据的用户 **林晓**（1997 年生，产品设计师）。她经历过一次错过晋升的转折（2026 年初），目前处于“重新定义成功”的阶段。

**产品结构（单页 SPA，无路由、无账号、无持久化）**：

| 视图 | 内容 |
|---|---|
| 首页 `home` | 林晓的个人首页：眉题“你的长期自我观察记录”；标题“看见那些反复发生的事，也看见那个正在变化的自己。”；两个直达按钮“我想找你聊聊”→“chat”、“查看我的人生之书”→“book”；右侧为品牌图形（细红路径汇聚成一条路 + 终点小人）；页脚只有一句安全边界短文案。 |
| 聊天 `chat` | 顶部两个平级 Tab（默认 **随手记**）：随手记（任何想法/事件/情绪）与深度访谈（自然追问价值观/世界观/人生观/反复模式，不显示固定问卷）。共享聊天框，支持 链接 / 照片 / AI 对话记录 / 普通文件 四种附件占位。左上角“☰ 历史记录”默认收起侧栏：随手记与深度访谈各有 2 条示例历史，可切换恢复内容（会话内状态，刷新不保留新增）。深度访谈每约 3 轮生成一条**待确认洞察**（价值观→世界观→人生观依次推进），确认/补充后写进“我是谁”。 |
| 人生之书 `book` | 左栏目录 + 移动端顶部章节点：第一章“我从哪里来”（副标题“我的人生编年表”，7 个节点，含“补充人生大事件”按钮，走 AI 复述确认后以“由你补充”样式进入编年表）；第二章“我是谁”（01 我的内在坐标：人生观/价值观/世界观；02 我反复出现的模式）；第三章“我在哪里”；第四章“我要去哪里”+ 计划页。 |

**语言**：中 / EN 一键切换（`中 / EN` 按钮），全部界面文案双语。

## 2. 从最初构想到本轮需求的完整时间线

> 早期对话发生在另外的 Codex 会话中，本仓库无法完整追溯原文；以下是依据 git 提交与本轮需求文档重建的事实性时间线，供接手者对齐。

1. **构想阶段（原始产品想法，早于本仓库）**：做一本“有证据、可纠正的人生之书”，用户可上传 ChatGPT 记录、日记、照片、普通文件等资料，由 AI 生成人生主线，而不是一套心理测评。
2. **第一版 Demo（commit `a9170e1`，2026-09-06）“Build InnerOS life book demo”**：实现了**首次体验路径**——首页（旧文案“一本属于你的人生之书 / 读懂你走过的路，看见你正在成为谁。”、“无需注册”等）→ 资料选择 → 生成动画 → 人生之书（旧四章）。提交时仓库仅含第一版；旧版本仍可在 git 历史中查看（`git show a9170e1:app/page.tsx`）。
3. **已有用户改造需求（本轮，即本文件依据的需求）**：产品状态改为“已有一定数据、可生成人生之书的用户 林晓”，砍掉首次注册/资料选择/生成动画路径；新增首页直达、聊天（随手记 + 深度访谈 + 历史侧栏）、人生之书扩展（编年表副标题 + 补充人生大事件 + 我是谁三观坐标）。正文见 `/Users/betty/Documents/ChatGPT/Life OS人生之书` 之外的原始需求文档。
4. **Codex 初步改造（未提交）**：`app/page.tsx`、`app/globals.css` 被整体重写为已有用户版；新增 `app/types.ts` 与 `app/api/inneros/route.ts`。此轮工作**未完成**：遗留 1 处 TypeScript 报错、`layout.tsx` 元信息仍是旧文案、测试仍指向已删除的骨架、未构建/未提交/未发布/未推送/未写交接文档。
5. **本交接版本（当前 HEAD 之后的完成态）**：修复类型错误；会话在 Tab/语言切换间不再丢失；洞察确认有稳定 id 且带“补充我的理解”；深度访谈按阶段出洞察；人生事件走 `/api/inneros` 的 `event_review`，缺时间/感受时 AI 追问；元信息、双语、移动端章节切换补齐；测试重写并可通过；生产构建通过；写入本文档并推送 GitHub（分支规则见 §8）。

## 3. 所有已确认决策及其原因

| 决策 | 原因 |
|---|---|
| 演示用户 = 林晓的转折之年（不再做首次生成路径） | 需求明确：产品要面向“已使用、已有数据”的存量用户，验证长期价值而非首次漏斗 |
| 首页极简 + 克制品牌图形（路径汇聚 + 小人），不铺满、不加照片 | 品牌图形即是 logo 意象，留白提升“长期记录工具”气质 |
| 首页文案“看见那些反复发生的事，也看见那个正在变化的自己。” | 相比旧文案更强调“反复模式”+“正在变化”，是产品价值主张 |
| “一本属于你的人生之书”等旧文案废弃；不出现“无需注册” | 旧文案抽象且指向首次体验；已有用户页不需要注册说明 |
| 聊天入口 = 顶部两个平级 Tab，默认随手记，而不是先选择模式再开聊 | 最低摩擦：用户进入即可记录；两模式复用同一聊天框与附件能力 |
| 历史侧栏默认收起，按模式分组展示 | 减少干扰；展开后可恢复历史会话 |
| 深度访谈不显示固定问卷，只自然追问，并按阶段产出“待确认洞察” | 体现“对话式自我观察”而非测试；尊重产品“不替用户下结论”的原则 |
| 洞察必须经用户确认/补充/否定后才进人生之书 | 人生之书的来源可信与用户主控是核心安全承诺 |
| 人生编年表 7 个节点（1997 童年 → 2026 至今） | 需求指定跨度：童年至今、≥7 节点；早期资料标注“用户回忆”，不伪装成数字记录 |
| “我是谁”拆成 内在坐标（人生观/价值观/世界观）+ 反复模式 | 需求指定结构；三观示例文案已写入 initialBeliefs |
| 单页状态式 Demo（screen/chapter/mode 用 useState），不加路由与数据库 | 需求明确：不新增账户、数据库、真实文件解析、跨设备持久化 |
| AI 服务统一入口 `/api/inneros`，有 `OPENAI_API_KEY` 才调真实模型，否则返回与林晓故事一致的结构化兜底 | 线上默认允许兜底；演示链路与真实链路一致 |
| 双语共用结构化文案字典（实现上为组件内 `lang` 三元） | 中英文一键切换、无遗漏混杂 |

## 4. 当前页面、交互、示例数据与技术结构

### 页面与交互（详见 §1 表格）

关键交互链路：
- 首页 `我想找你聊聊` → 随手记 Tab；`查看我的人生之书` → 编年表章。
- 聊天 → `☰ 历史记录` 展开侧栏（移动端为抽屉覆盖层）→ 点任意历史切换会话。
- 深度访谈持续对话：发 1 条、4 条、7 条…（每次触发前一条被确认/否定/补充后）时，出现“待确认洞察”卡片（价值观 → 世界观 → 人生观依次推进）；`符合我` 或 `补充我的理解（保存并确认）` → 写入“我是谁”；`不太符合` → 丢弃。
- 人生之书 → 第一章 `＋补充人生大事件` → 弹层表单（时间必填、事件必填、感受可空、来源可选）→ “让 AI 帮我整理”调 `/api/inneros event_review` 复述 → 展示 AI 复述；若感受为空，追问“那时的你，是怎么理解这件事的？”并提供补充输入 → `确认加入编年表` → 以红边“由你补充 · 已确认”卡片插入当前编年表（仅当前会话）。
- “我是谁”坐标卡各带 `符合我 / 不符合 / 补充` 反馈按钮；深度访谈确认的新洞察会立即出现在坐标区。

### 示例数据（硬编码于 `app/page.tsx`）

- `baseTimeline`：7 个编年表节点（zh/en）。
- `initialBeliefs`：人生观/价值观/世界观三卡，来源标“长期记录归纳”或“深度访谈待确认”。
- `insightStages`：3 条深度访谈候选洞察（按 kind 循环）。
- `seedHistories`：随手记 ×2、深度访谈 ×2（zh/en 各 4 段会话与消息）。
- 首页右下 `18 段记录 · 7 个人生节点 · 3 个反复模式` 为文案性数字。

### 技术结构

```
app/
  layout.tsx            根布局 + SEO/OG 元信息（新定位文案）
  page.tsx              全部单页视图（Home/Chat/Book/EventModal…），会话内状态
  globals.css           全部样式（Tailwind v4 + 手写 CSS 变量主题）
  types.ts              最小公共类型：ChatMode/Conversation/ChatMessage/
                        Attachment/LifeEventDraft/ConfirmedLifeEvent/BeliefInsight
  api/inneros/route.ts  AI 统一入口：chat（随手记/深度访谈）与 event_review
  chatgpt-auth.ts       （starter 自带，未使用：可选 ChatGPT 登录）
public/og.png           OG 分享图（旧首版视觉，可后续替换）
tests/rendered-html.test.mjs   SSR 冒烟 + 产物内容断言（pnpm test 使用）
worker/index.ts         vinext worker（starter，未改动）
.openai/hosting.json    OpenAI Sites 托管声明（project_id 已存在）
```

状态归属：页面状态在 `Home()` 顶部用 useState 集中管理；聊天会话以 `sessions[lang][conversationId]` 保存，切换 Tab/语言不丢；语言切换分别保留两套会话与两套内在坐标。

## 5. 本地启动、构建、AI 密钥与部署

```bash
pnpm install            # 或 pnpm install --frozen-lockfile
pnpm run dev            # 本地开发 http://localhost:3000
pnpm run build          # vinext 生产构建 → dist/（可离线，首次会缓存字体到 .vinext/fonts）
pnpm test               # 先 build 再跑 SSR 冒烟测试
./node_modules/.bin/tsc --noEmit   # 类型检查
```

- **AI 密钥**：可选。设置环境变量 `OPENAI_API_KEY`（及可选 `OPENAI_MODEL`，默认 `gpt-5.2`）后，`/api/inneros` 调用真实模型；未设置或超时/出错时自动返回与林晓故事一致的兜底文案（响应带 `fallback: true`）。线上默认允许兜底，无需配置密钥即可体验完整演示链路。
- **部署（OpenAI Sites）**：本仓库经 Codex “Sites” 流程发布，`project_id: appgprj_6a9cfdc63cd08191bb3acfba6c6d93f2`。在具备发布权限的 Codex 环境中执行 `pnpm run build` 并发布站点（Sites 发布需要 OpenAI 环境授权，普通终端无法完成；如授权可用，直接对当前 main 重新发布即可）。
- **本机 UI 验证建议**：`pnpm run dev` 后走一遍 §4 的链路；无浏览器自动化时可用 `curl http://localhost:3000` 看 SSR 首页，再手动点按验证客户端交互。

## 6. 已发布地址、GitHub 地址与分支状态

- **已发布地址**：由 OpenAI Codex Sites 托管；请以 ChatGPT/Codex 中该项目的最新发布为准（旧首版曾发布，本轮完成后需**重新发布**以替换旧版）。
- **GitHub**：`https://github.com/qingdou-1919/-.git`（本机已配置 `origin`）。
- **分支状态（2026-09-06 收尾时快照）**：
  - 本地 `main` 历史：`044de76`（远程初始 stub，仅 README“# -”）→ `c0fddd3`（已有用户版完成）→ `7b66e1b`（与远程 stub 的无关联历史合并，保留原 README 内容于文首）。
  - 推送规则执行情况：远程非空 → 已 fetch 并 `--allow-unrelated-histories` 合并、保留原内容；**尚未 push** —— 会话环境对 github.com 的出网连接中途被断（fetch 成功、push 时 TCP 443 超时）。
  - 接手时只需在本机（或任何有 GitHub 网络与凭据的环境）执行：`git push origin main`。

## 7. 本次会话收尾状态（附注）

- ✅ TypeScript `tsc --noEmit` 通过；`pnpm run build` 通过；`pnpm test`（SSR 冒烟 + 产物断言）5/5 通过。
- ✅ `app/layout.tsx` 元信息、全部双语界面、移动端章节切换、会话内跨 Tab/语言保留、AI 统一入口（chat + event_review，含缺失感受追问）已完成。
- ⏳ **GitHub push**：`git push origin main` 被当前环境网络阻断（外部阻塞项，符合预期流程中“仅报告、不改仓库”的处理）；本地合并已就绪。
- ⏳ **Sites 重新发布**：需要具备发布授权的 Codex/OpenAI 环境执行；本机无法完成。构建产物 `dist/` 已就绪（被 `.gitignore` 忽略、不随仓库推送）。
- 验证建议：`pnpm run dev` 后人工走一遍 首页 → 聊天（随手记/深度访谈/历史/洞察确认）→ 人生之书（编年表/补充事件/我是谁）→ 中英切换。


## 7. 已知限制、未完成事项与推荐执行顺序

### 已知限制（有意为之，符合本轮范围）
1. 无账户/数据库/真实文件解析/跨设备持久化；刷新后用户新增（聊天消息、事件、确认的洞察）会回到示例初始状态。
2. 附件按钮只生成占位卡片，不做真实上传解析。
3. 深度访谈的“待确认洞察”文案是阶段预设（未做真实抽取），但确认/否定/补充链路与真实一致。
4. 双语以两套硬编码数据实现；同一用户新确认的洞察不会自动翻译到另一种语言。
5. 聊天 Enter 发送、Shift+Enter 换行；无更复杂富文本。
6. `public/og.png`、`favicon.svg` 仍是首版视觉，与新版品牌（红路径+小人）不完全一致。
7. 聊天只展示“示例历史”会话，暂不能新建空白会话/重命名。

### 未完成事项（推荐顺序）
1. **推送 GitHub `main`**：`git push origin main`（本环境出网被断；本地合并已完成、仓库内容即最终版）。
2. **重新发布 Sites**（需要 OpenAI 环境授权）并更新 §6 实际地址。
3. 更新 `public/og.png` / `favicon.svg` 为新品牌图形（路径汇聚 + 小人）。
4. 视觉回归：在 ≤900px / ≤620px 下过一遍 首页→聊天→人生之书→事件补充 全链路（本章节补充了移动端章节点与抽屉式历史）。
5. （可选）把“演示人生/示例数据”升级为“新建会话”以更贴近真实产品。
6. （可选）深度访谈洞察改为由模型根据真实对话内容提炼（route 已留好结构化入口）。
7. 第 6 条双语一致性：可把全部文案收敛进单一词典文件（当前为组件内三元，结构化但分散）。
